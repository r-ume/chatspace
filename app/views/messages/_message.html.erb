<li>
    <%= message.body %>  <strong>sent by</strong>  <%= message.user.name %>
    <%= message.created_at.strftime("%Y年%m月%d日 %H時%M分") %>
    <% if message.image %>
      <%= image_tag message.image.url %>
    <% end %>
</li>
