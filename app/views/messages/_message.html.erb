<h1>
  <%= @chat_group.name %>
</h1>

<p>
  <% @chat_group.users.each do |user| %>
    <%= user.name %>,
  <% end %>
</p>

<li>
  sent by <%= message.user.name %>
  <ul>
    <%= message.body %>
    <%= message.created_at.strftime("%Y年%m月%d日 %H時%M分") %>
  </ul>
</li>
