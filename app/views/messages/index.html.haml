-#%header
-#  %p.header-text チャットグループが作成されました。

.flash__message
  %p.flash__message__text チャットグループが作成されました。

.contents
  .group
    .group__user
      - if user_signed_in?
      %p.group__user__name
        = @chat_group.name

    .group__contents
      .group__contents__box
        - @chat_groups.each do |chat_group|
          %p.group__contents__box__name
            = chat_group.name
            %p.group__contents__box__message 最初のメッセージ

  .chat
    .chat__group
      %p.chat__group__name
        = @chat_group.name
      .chat__group__edit Edit
    - @chat_group.users.each do |user|
      %p.chat__group__member
      = user.name

  .contents
    = form_for [@chat_group, @message], html: { class: 'form js-form', name: 'the-form' } do |f|
      = f.text_area :body, class: 'form__text-field js-form__text-field', placeholder: 'message'
      = f.file_field :image
      = f.submit class: 'form__submit js-submit'

    %ul.messages_list
      = render @messages

  = link_to "sign out", destroy_user_session_path, method: :delete

    -#- else
    -#    %h1 you need to log in
    -#    = link_to "sign in", user_session_path, method: :post